# Smart Task Manager – Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────
fastapi==0.115.9           # Modern async web framework (ASGI)
uvicorn[standard]==0.34.0  # ASGI server (includes websockets + http tools)

# ── Database ───────────────────────────────────────────────────────────────
sqlalchemy==2.0.40         # ORM + core SQL toolkit
# aiosqlite==0.21.0        # Uncomment if you switch to async SQLAlchemy

# ── Data Validation ────────────────────────────────────────────────────────
pydantic==2.11.3           # Request/response schema validation (v2)
pydantic-settings==2.9.1   # Load settings from environment / .env files
email-validator==2.2.0     # Enables Pydantic's EmailStr type

# ── Security – Password Hashing ────────────────────────────────────────────
passlib[bcrypt]==1.7.4     # Password hashing library; [bcrypt] extra required
bcrypt==4.3.0              # bcrypt backend for passlib (NFR8)

# ── Security – JWT ─────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0  # JWT encode/decode with HS256 support

# ── Form Parsing (OAuth2PasswordRequestForm) ───────────────────────────────
python-multipart==0.0.20   # Required by FastAPI to parse application/x-www-form-urlencoded

# ── Development / Testing (optional) ──────────────────────────────────────
# pytest==8.3.5
# httpx==0.28.1             # Async test client for FastAPI
# pytest-asyncio==0.25.3
